# 复制成 .env.local 后填入真实值（.env.local 已被 .gitignore 挡住，不会进仓库）：
#   cp .env.example .env.local
#
# 读取方：
#   - scripts/upload-frames.mjs、scripts/build-data.mjs 经 scripts/load-env.mjs 自动加载
#   - npm run build（Next）自身也会自动读 .env.local，故 NEXT_PUBLIC_* 无需再 export
# 已导出的真实环境变量优先级高于本文件（CI secrets 不会被覆盖）。

# ── R2 上传（scripts/upload-frames.mjs）───────────────────────────────────────
# 默认走 wrangler 后端：只需桶名 + 一次 `npx wrangler login`（部署 Pages 本就要做），
# 不必装 AWS CLI、不必建 R2 API Token。
R2_BUCKET=aiconf-frames

# 可选：文件量大时可改用 aws s3 sync 批量同步（更快，且会删除远端多余对象）。
# 需本机装有 aws CLI，并在 R2 → Manage R2 API Tokens 建 Token（Object Read & Write）。
# 四项配齐且 aws 可用时自动启用；这些是密钥，切勿提交。
# R2_ENDPOINT=https://<accountid>.r2.cloudflarestorage.com
# AWS_ACCESS_KEY_ID=<Access Key ID>
# AWS_SECRET_ACCESS_KEY=<Secret Access Key>

# ── 站点构建（NEXT_PUBLIC_* 会被内联进静态产物，非密钥）──────────────────────
# 关键画面的公开访问根地址，不带尾斜杠。自定义域名如 https://img.example.com，
# 或 R2 免费域名 https://pub-xxx.r2.dev。留空则回落本地 /frames/（仅本地开发用）。
NEXT_PUBLIC_FRAMES_BASE=

# 站点最终域名，决定 OG/Twitter 卡片里的绝对地址与 metadataBase。
# Cloudflare Pages 为 https://<项目名>.pages.dev，或你的自定义域名。
NEXT_PUBLIC_SITE_URL=

# ── 数据源（默认 file 读本地 JSON；未来接 DB 时才需要）───────────────────────
# DATA_SOURCE=file
# DATA_API_URL=
