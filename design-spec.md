# AI Conference 2026 Compass — 设计规范（design-spec）

> 版本 v1.0 · 负责人 Jobs（产品设计）· 收件人 Elon（验收）· 实现 Linus
> 状态：待 Elon 验收。本规范为自包含设计说明，实现前请以本文档 + 链接的 Stitch 参考稿为准。

---

## 0. 一页速览（给评审）

- **产品定位**：一份"有编辑判断的 AI 大会情报专题 / 学习导航"。不是视频平台、不是媒体门户、不是 SaaS 控制台。
- **核心动作**：帮从 X 点进来、停留时间短的用户，在 **10 秒内** 找到"我这类人、我这点时间，该先看哪几条"。
- **一句话价值**：300 小时 AI 大会视频，不必全看。
- **气质**：克制、可信、编辑感强；像高质量技术杂志 / 研究报告 / 现代产品文档。中英文混排优先阅读。
- **硬约束**：所有视频**跳转官方来源**，本站不播放、不托管。
- **交付范围**：信息架构 · 桌面/手机首页 · 目录页 · 观点卡/视频详情模块 · 基础设计系统 · 交互说明。

---

## 1. 目的、受众与设备类型（Purpose / Audience / Device）

### 1.1 目的
把三场大会（AI Engineer World's Fair 2026、Cursor Compile 2026、Figma Config 2026）约 300 小时公开视频，整理为"可按**角色 / 主题 / 时间预算**消费"的导航，用编辑判断替用户做减法。

### 1.2 受众（按优先级）
1. **短停留的社交流量**（从 X/Twitter 点入）：期待立刻得到答案，一屏内看到"给我看什么"。**第一优先级，决定 Hero 与首屏。**
2. **开发者**：想跟进 AI 进展但没时间看完。
3. **产品经理 / 设计师 / 创业者 / 技术负责人**：按角色取用。

### 1.3 设备类型
- **移动端优先**（375–430px 为主要设计基准，社交流量以手机为主）。
- 桌面端（≥1024px）为"深度浏览/目录检索"场景做增强。
- 平板按桌面窄栏降级，不单独设计。
- 支持范围：现代移动 Safari / Chrome、桌面 Chrome/Safari/Edge。不支持 IE。

### 1.4 内容语态
中文优先，专有名词与讲者名保留英文原文（如 Agent、Evals、Context、Design-to-Code、讲者姓名、大会英文名）。所有正文排版按"中英文混排"处理（见 §7.2）。

---

## 2. 信息架构（Information Architecture）

### 2.1 站点地图
```
首页 /
├── Hero + 三大会来源 + 角色入口 + 时间预算入口
├── 本周最值得看的 10 个观点（编辑精选）
├── 按主题探索（6 主题）
└── CTA：主「在 X 上关注更新」· 次「加入候选名单」· 「查看完整目录」

完整目录 /catalog
├── 概览统计条（已索引 session / 总时长 / 已深度解读数）
├── 筛选区（大会 · 角色 · 主题 · 时长 · 状态）
└── 视频卡列表（默认按"推荐先看"权重排序）

视频详情 /video/{id}
├── 视频元信息 + "为什么值得看"编辑解读
├── 关键观点（观点卡）
├── 跳转官方来源按钮（YouTube/官方）
└── 相关推荐（同主题/同讲者）

角色落地 /for/{role}         （developer | product-design | founder-lead | trend）
时间预算落地 /budget/{t}      （10min | 1h | 3h → 预置行程/歌单式清单）
主题落地 /topic/{slug}        （agent | ai-coding | evals | context | design-to-code | ai-product）
候选名单 /waitlist            （每周精选邮件筹备中；收集邮箱，上线后通知，不伪装为已上线订阅）
关于/方法论 /about            （编辑标准、状态定义、数据来源与免责声明）
```

### 2.2 导航模型
- **顶栏（全局）**：Logo「Compass」· 目录 · 主题 · 关于 · 主 CTA「在 X 上关注更新」。手机端收敛为 Logo + 「关注 X」+ 汉堡/底部。

> **CTA 决策（董事会已锁定，2026-07-14）**
> - **主 CTA：「在 X 上关注更新」** —— 唯一主行动，指向 X 账号，全站顶栏与 CTA 收束区一致。
> - **次 CTA：「加入候选名单」** —— 每周精选邮件后端尚未实现；按钮文案为「加入候选名单」，配一行说明「每周精选邮件筹备中，上线后通知你」，**不得**使用「订阅每周精选」等暗示功能已上线的措辞。待邮件后端上线后，再将次 CTA 文案切回「订阅每周精选」。
> - **禁止**任何未经验证的社会证明（如「12,000+ 用户」）。
- **首页即导航中枢**：三条主要进入路径 = 角色维度、时间维度、主题维度。这三条是并列的"切法"，用户任选其一都能到达目录的一个已筛选视图。
- **面包屑**：详情页与落地页显示 `目录 / {主题|角色} / 当前`。
- **URL 语义化**，落地页本质是"目录的预设筛选视图 + 一段编辑导语"。

### 2.3 内容对象模型（供 Linus 建模参考，非最终数据库设计）
- **Video/Session**：id、标题、讲者[]、大会、时长(min)、主题[]、状态、官方链接、发布日期、`whyWatch`（为什么值得看，编辑文案）、`takeaways[]`（观点卡）。
- **Takeaway（观点卡）**：id、videoId、一句话观点、支撑上下文、时间戳(可选，跳官方源指定时刻)、适用角色[]。
- **Conference**：AI Engineer / Cursor Compile / Figma Config（名称、色标、logo）。
- **状态枚举**：`recommended`(推荐先看) · `indexed`(已收录) · `analyzing`(解读中)。
- **角色枚举**：`developer` · `product-design` · `founder-lead` · `trend`。
- **主题枚举**：`agent` · `ai-coding` · `evals` · `context` · `design-to-code` · `ai-product`。

---

## 3. 基础设计系统（Design System）

### 3.1 设计原则
1. **阅读优先**：正文列宽受控，行高宽松，信息层级靠字号/字重/间距而非颜色堆叠。
2. **克制用色**：暖白纸面 + 近黑墨字 + 单一深墨蓝主强调色；主题/大会用低饱和色标，仅做识别不做装饰。
3. **编辑感**：衬线标题 + 无衬线正文 + 等宽体做数据/元信息；细发丝线分隔；无花哨渐变、无霓虹、无过度圆角。
4. **状态清晰**：三种视频状态视觉可区分，"推荐先看"是全站视觉锚点。

### 3.2 颜色（Palette）
明亮模式为默认，采用暖白"纸面"基调。

**中性 / 纸面**
| Token | Hex | 用途 |
|---|---|---|
| `--paper` | `#FBFAF8` | 页面底色（暖白纸面） |
| `--surface` | `#FFFFFF` | 卡片/浮层表面 |
| `--surface-sunken` | `#F4F2ED` | 次级块、筛选栏底 |
| `--ink` | `#1C1B19` | 主文本（近黑，微暖） |
| `--ink-secondary` | `#57534E` | 次级文本 |
| `--ink-muted` | `#8A857D` | 元信息/占位 |
| `--hairline` | `#E7E4DE` | 分隔线/卡片描边 |
| `--hairline-strong` | `#D8D4CC` | 输入框/强分隔 |

**主强调（Editorial Ink Blue）**
| Token | Hex | 用途 |
|---|---|---|
| `--accent` | `#1B3A5B` | 主 CTA、链接、选中态 |
| `--accent-hover` | `#142C46` | 主色 hover |
| `--accent-tint` | `#E9EEF4` | 主题 chip/选中背景 |

**状态色（低饱和，语义化）**
| 状态 | 文字 | 底色 tint | 说明 |
|---|---|---|---|
| 推荐先看 `recommended` | `#8A5200` | `#FBEFD6` | 暖金；全站视觉锚点，配 ★ 图标 + 卡片左侧 3px 强调条 |
| 已收录 `indexed` | `#57534E` | `#F0EEE9` | 中性；已入库但未深读 |
| 解读中 `analyzing` | `#2F5D50` | `#E4EFE9` | 深绿；配脉冲点表示进行中 |

**大会色标（仅识别，6px 圆点 + 文字，禁止大面积铺色）**
| 大会 | Hex |
|---|---|
| AI Engineer World's Fair | `#2F5D50`（森林绿） |
| Cursor Compile | `#3A3A3A`（石墨黑） |
| Figma Config | `#6B4E9E`（低饱和紫） |

**反馈色**：错误 `#9B2C2C` / 底 `#F7E9E7`；成功 `#2F5D50` / 底 `#E4EFE9`。

> 深色模式：v1 **非目标**（见 §9），仅预留 token 命名。若 Elon 要求纳入，追加一版反相纸面（`--paper:#171614` / `--ink:#EDEAE3`）。

### 3.3 字体与字号（Typography）
**字族（中英文混排）**
- 标题/展示（衬线，编辑感）：`Source Serif 4` / `Newsreader`（EN） + `Noto Serif SC 思源宋体`（CN）。
- 正文/UI（无衬线，可读）：`Inter`（EN） + `Noto Sans SC 思源黑体`（CN）。
- 数据/元信息（等宽，报告感）：`IBM Plex Mono` / `JetBrains Mono`（时长、计数、讲者标签、时间戳）。
- font-family 回退链示例：`"Source Serif 4","Noto Serif SC",Georgia,serif`。

**字阶（桌面 / 手机，px）**
| 级别 | 桌面 size/line/weight | 手机 | 字族 |
|---|---|---|---|
| Display (Hero H1) | 56 / 1.08 / 600 / -0.02em | 34 / 1.12 / 600 | 衬线 |
| H2 区块标题 | 32 / 1.2 / 600 | 24 / 1.25 / 600 | 衬线 |
| H3 卡片标题 | 20 / 1.35 / 600 | 18 / 1.35 / 600 | 无衬线 |
| Body L 导语 | 18 / 1.65 / 400 | 16 / 1.6 | 无衬线 |
| Body M 正文 | 16 / 1.6 / 400 | 15 / 1.6 | 无衬线 |
| Body S 说明 | 14 / 1.5 / 400 | 13 / 1.5 | 无衬线 |
| Label 标签 | 13 / 1.4 / 500，EN 大写 tracking .06em | 12 | 无衬线 |
| Meta 元信息 | 13 / 1.4 / 500 | 12 | 等宽 |

正文阅读列宽上限 **68–72 字符（≈680–720px）**；标题列宽上限 20 字。

### 3.4 间距与栅格（Spacing / Grid）
- **间距刻度（4px 基）**：4 · 8 · 12 · 16 · 24 · 32 · 48 · 64 · 96。
- **桌面栅格**：12 列，内容最大宽 1200px，列间距 24px，页边距 32px。
- **手机栅格**：单列/双列混合，页边距 16px，卡片间距 12–16px。
- **区块垂直节奏**：区块间 64–96px（桌面）/ 40–56px（手机）。

### 3.5 卡片（Card）
- 圆角 **8px**（克制，不超 8）；描边 `1px var(--hairline)`；背景 `--surface`；内边距 20–24px。
- 阴影：默认 `0 1px 2px rgba(0,0,0,.04)`；hover `0 6px 20px rgba(0,0,0,.08)` + 上移 2px（150ms ease）。
- **推荐先看卡**：左侧 3px `--accent`/暖金强调条 + ★ 徽标，是列表里的视觉锚点。
- 禁止：大圆角（>12）、彩色渐变卡背、强投影、卡片内多主色并置。

### 3.6 标签与徽章（Tags / Badges）
- **主题 chip**：圆角 4px，13px，`--accent-tint` 底 + `--accent` 字，可点击进入主题落地页。
- **大会标**：6px 圆点（大会色）+ 大会名（Body S）。
- **状态徽章**：见 §3.2 三态；圆角 4px，含图标（★ / 圆点 / 脉冲点）。
- **时长标**：等宽体，如 `18 min` / `1 h 02`，配时钟微图标。

### 3.7 按钮与表单
- **主按钮**：实心 `--accent`，白字，圆角 6px，高 44px（触控友好），hover 加深。
- **次按钮**：描边 `--hairline-strong`，`--ink` 字，透明底。
- **文字按钮/链接**：`--accent`，hover 下划线。
- **输入/筛选控件**：高 40px，圆角 6px，`1px --hairline-strong`，focus 时 `--accent` 描边 + 2px 外发光 `--accent-tint`。
- 最小可点区 44×44px（移动端）。

### 3.8 图标与图形
线性图标（1.5px 描边），单色随文本色。**不使用 3D、拟真、AI 生成插画或霓虹光效**。允许极简几何指北针 mark 作为 Logo。

---

## 4. 首页（Home）— 桌面端 + 手机端

### 4.1 区块清单（自上而下）与信息层级
1. **顶栏**（sticky）：Logo「◮ Compass」+ 导航（目录/主题/关于）+ 主 CTA「在 X 上关注更新」。
2. **Hero**
   - H1（Display，衬线）：**「300 小时 AI 大会视频，不必全看」**
   - 副标题（Body L）：一句话说明"我们用编辑判断，按你的角色和时间，挑出该看的几条"。
   - 三大会来源标识：`AI Engineer · Cursor Compile · Figma Config`（色标圆点 + 名称，横排一行；手机换行为两行）。
   - 首屏内出现主 CTA + 一行统计。**统计数字必须取自 `data/catalog.json` 真实语料**：当前「已索引 941 场 · 总时长 409 小时 · 深度解读进行中」。`whyWatch` 深度解读当前为 0，未产出真实条数前显示「进行中」而非具体数字，严禁沿用占位数。
   - 视觉：纯纸面底 + 一条细发丝线收边，无大图/无渐变。
3. **角色入口**（4 卡一行 / 手机 2×2）：开发者 · 产品/设计 · 创始人/负责人 · 只想跟进趋势。每卡：角色名 + 一句"给你看什么" + 进入指示 → `/for/{role}`。**桌面与手机卡结构必须一致**（手机不得退化为纯图标+名称的空壳）。角色↔视频的映射尚无真实标注，故**不显示编造的"N 内容"角标**；待 Linus 建立角色标签后再回填真实计数。
4. **时间预算入口**（3 卡一行 / 手机横滑或竖排）：**10 分钟 · 1 小时 · 3 小时**。每卡：时间 + "能看完 N 条精华" + 副标"像歌单一样安排好" → `/budget/{t}`。
5. **本周最值得看的 10 个观点**（编辑精选区）
   - 区标 H2 +「查看完整目录 →」。
   - 10 张**观点卡**（§6.1），桌面 2 列、手机 1 列。序号 01–10（等宽体，编辑榜单感）。
   - 每卡视觉包含"为什么值得看"一句 + 来源视频 + 大会 + 时长 + 状态。
6. **按主题探索**（6 格）：Agent · AI 编程 · Evals · Context · Design-to-Code · AI 产品。每格：主题名 + 近似计数 + 一句概述 → `/topic/{slug}`。**计数为按标题关键词的近似归类**（当前约 Agent 262 · AI 编程 101 · Evals 70 · Context 75 · Design-to-Code 64 · AI 产品 61，取自 `data/catalog.json`），须以「约」标注，避免读作精确编目；正式主题标签由 Linus 在 build 阶段建立。
7. **CTA 收束区**：主「在 X 上关注更新」（指向 X 账号）+ 次「加入候选名单」（邮箱输入框 + 「加入候选名单」按钮 + 一行「每周精选邮件筹备中，上线后通知你」说明）+ 「查看完整目录」次按钮。**不含**任何用户数/社会证明数字。
8. **页脚**：关于/方法论、数据来源与免责声明（"所有视频跳转官方来源，本站不托管"）、三大会官方链接、版权。

### 4.2 桌面端布局（≥1024px）
- 12 列，内容居中最大 1200px。
- Hero：左对齐文本块占 8 列，右侧留白（或放"本周精选 Top1"小卡）。
- 角色 4 卡等宽（每卡 3 列）；时间 3 卡等宽（每卡 4 列）。
- 精选区 2 列瀑布；主题 3×2 网格。

### 4.3 手机端布局（375–430px）
- 单列，页边距 16px。
- 顶栏收敛：Logo + 「关注 X」文字按钮；导航进底部或汉堡。
- Hero：H1 34px；三大会来源两行；主 CTA「在 X 上关注更新」满宽 44px。
- 角色 2×2；时间预算竖排或横向 snap 滑动；精选观点卡单列；主题 2 列网格。
- **底部常驻栏（可选）**：`目录` / `主题` / `候选名单` 三入口，48px 高，便于短停留用户单手操作。（是否上线见 §9 待定项）

### 4.4 首屏承诺（社交流量关键）
移动端首屏（无需滚动）必须同时看到：H1 承诺 + 三大会来源 + 至少"角色入口或时间入口之一" + 一个可点 CTA。目标：**点进来 5 秒内知道"这是什么、给我看什么"**。

---

## 5. 视频目录页（Catalog）

### 5.1 区块与层级
1. **页头**：H1「完整目录」+ 一句编辑导语。
2. **概览统计条**（等宽体，报告感）：`已索引 941 场 session · 总时长 409 小时 · 深度解读进行中`。三个数字并排，配细分隔线。**数值取自 `data/catalog.json`（941 场 / 409 小时 / whyWatch 深度解读现为 0）**；深度解读达成前显示「进行中」，不显示编造条数。来源分布 AI Engineer 853 · Figma Config 78 · Cursor Compile 10（极不均，稀疏来源须接空状态，见 §7）。
3. **筛选区**（sticky，桌面左栏 / 手机顶部抽屉）：
   - **大会**：AI Engineer / Cursor Compile / Figma Config（多选）。
   - **角色**：开发者 / 产品·设计 / 创始人·负责人 / 趋势（多选）。
   - **主题**：6 主题（多选 chip）。
   - **时长**：`<15min` / `15–40min` / `>40min`（单选段）。
   - **状态**：推荐先看 / 已收录 / 解读中（多选）。
   - 顶部显示"结果计数"与「清除筛选」；筛选即时生效（见 §8）。
4. **排序**：默认「推荐优先」；可切「最新」「时长短→长」。
5. **视频卡列表**：默认权重把 `recommended` 卡置顶并视觉加重；`indexed` 卡可见但克制；`analyzing` 卡带进行中态。
6. **分页/加载更多**：无限滚动 + "加载更多"兜底按钮。

### 5.2 视频卡（列表项）字段（全部必含）
标题 · 讲者 · 大会（色标）· 时长 · 主题(chips) · 状态徽章 · **为什么值得看**（1–2 行编辑摘要）。次要：发布日期、观点条数。整卡可点进详情；卡内"官方来源 ↗"按钮直达官方。

### 5.3 视觉重点规则（硬要求）
- **允许浏览未深读视频**，但视觉重量分级：`recommended`（左强调条 + ★ + 更高对比）> `indexed`（常规）> `analyzing`（低饱和 + 进行中点）。
- 未深读（`indexed`）不隐藏、不置灰到不可读，但不与推荐同权重争夺注意力。

---

## 6. 观点卡 / 视频详情模块

### 6.1 单条观点卡（Takeaway Card）— 首页精选 & 详情页复用
- **结构**（自上而下）：
  1. 序号/来源行：`01` 榜单序号（首页）· 大会色标 · 状态徽章。
  2. **观点句**（H3，1–2 行）：编辑提炼的一句话结论，是卡片主角。
  3. 支撑说明（Body S，最多 2 行）：为什么值得看 / 关键上下文。
  4. 元信息行（等宽 Meta）：讲者 · 时长 · 主题 chips。
  5. 行动：`看官方片段 ↗`（带时间戳跳官方源）+ `展开视频详情`。
- **状态**：默认 / hover（上浮 + 阴影 + 行动区显现）/ focus（键盘可达，`--accent` 焦点环）/ loading（骨架）/ empty（"本周精选整理中"）/ error（"加载失败，重试"）。

### 6.2 视频详情页模块（/video/{id}）
- **头部**：标题（H2 衬线）· 讲者 · 大会色标 · 时长 · 主题 chips · 状态徽章 · 发布日期。
- **主行动**：实心「在官方来源观看 ↗」（YouTube/官方）；副文案"本站不播放，跳转官方"。
- **为什么值得看**：一段编辑解读（Body L/M），本页的核心增值内容。
- **关键观点列表**：若干观点卡（§6.1），可带时间戳深链官方源具体时刻。
- **元信息侧栏/块**：大会、角色适配、主题、时长、来源链接、深度解读状态。
- **相关推荐**：同主题/同讲者 3–6 条，复用视频卡。
- **面包屑**：`目录 / {主题} / 当前`。
- 若状态为 `analyzing`：展示"解读中"占位——已有元信息 + "深度解读整理中，先看官方原片"，避免空白。

---

## 7. 数据、加载与缺失行为 + 混排规则

### 7.1 数据要求与状态行为
| 场景 | 加载中 | 空数据 | 错误 |
|---|---|---|---|
| 首页精选/主题/角色 | 骨架卡（灰阶 shimmer，尊重版式，不跳动） | "本周精选整理中，先去完整目录看看 →" | "内容加载失败" + 重试；顶栏与导航仍可用 |
| 目录列表 | 前 6 张骨架卡 | 筛选无结果：插画留白 + "没有匹配项，试试放宽筛选" + 「清除筛选」 | 行内错误条 + 重试，保留已选筛选 |
| 详情"为什么值得看" | 段落骨架 | 状态=解读中：显示占位文案 + 官方链接 | 重试；元信息与官方链接优先渲染 |
| 统计条 | 数字位骨架 | 显示 `—` 占位 | 静默降级为 `—`，不阻塞页面 |

原则：**官方来源链接与基础元信息永远优先渲染**，编辑增值内容（whyWatch/观点卡）可懒加载/降级；任何模块失败不得导致整页白屏。

### 7.2 中英文混排规则
- 中英文之间自动 1/4 空格（用 CSS `text-spacing` 或排版处理），标点用中文全角、代码/英文用半角。
- 英文专有名词、讲者名、主题英文（Agent/Evals/Context/Design-to-Code）保持原文原字重，不强制大写（Label 标签除外）。
- 数字、时长、计数一律等宽体，避免跳动。
- 行末避免孤字；标题允许软断行控制。

---

## 8. 页面交互说明（Interaction）

### 8.1 全局
- **顶栏**：桌面常驻；手机上滑隐藏、下滑显现（sticky 智能收起）。主 CTA 常在。
- **焦点/键盘**：所有可点元素 Tab 可达，focus 环 `2px --accent`；Esc 关闭浮层/抽屉。
- **动效基调**：150–200ms ease-out，仅用于位移/透明度；**无弹跳、无视差、无自动播放**。尊重 `prefers-reduced-motion`。

### 8.2 首页
- **角色卡 / 时间卡 / 主题格**：hover 上浮 + 描边加深；点击 → 对应落地页（= 目录预设筛选视图 + 导语）。
- **时间预算卡**：点击进入"歌单式"清单页，按所选时间预算装配可看完的条目并显示累计时长进度。
- **精选观点卡**：hover 显现行动区；「看官方片段 ↗」新标签打开官方源（带 `rel=noopener`）；「展开」进详情。
- **订阅**：邮箱输入 → 提交 loading → 成功 toast（success 态）/ 失败行内错误。

### 8.3 目录页
- **筛选即时生效**：任一筛选变更 → 列表淡入更新 + 结果计数刷新；筛选状态写入 URL query（可分享/可回退）。
- **多选 chip**：点选切换选中态（`--accent-tint` 底）；「清除筛选」一键复位。
- **手机筛选**：顶部「筛选」按钮 → 底部抽屉（全屏可滚），底部「应用（N 条结果）」实时预览计数。
- **排序**：下拉切换即时重排。
- **加载更多**：滚动到底自动加载 + 兜底按钮；加载时追加骨架卡。
- **卡片行为**：整卡点击进详情；「官方来源 ↗」阻止冒泡、直达官方。

### 8.4 详情页
- 「在官方来源观看 ↗」→ 新标签打开官方链接（本站绝不内嵌播放器）。
- 观点卡时间戳链接 → 打开官方源对应时刻（`&t=` 深链）。
- 相关推荐点击 → 平滑进入下一条详情，面包屑更新。

### 8.5 反馈与边界
- 所有异步操作有明确 loading / success / error 三态。
- 外链统一新标签 + `↗` 图标 + `noopener`；对用户明示"离开本站前往官方来源"。
- 断网/接口错误：非阻塞行内提示 + 重试，已加载内容保留。

---

## 9. 非目标与待 Elon/董事会决策项

### 9.1 明确非目标（Non-goals）
- 不做站内视频播放/托管（仅跳官方源）。
- 不做用户账号体系、评论、社交互动、收藏夹（v1）。
- 不做视频转写全文/字幕检索（v1，可作为后续）。
- 不做深色模式（v1，仅预留 token）。
- 不做 SaaS 控制台式仪表盘、复杂个性化推荐引擎（编辑精选优先于算法）。

### 9.2 待决策（需 Elon 或董事会拍板）
1. ~~每周精选获取方式~~ **（已决策 2026-07-14）**：主 CTA 定为「在 X 上关注更新」，次 CTA 定为「加入候选名单」（邮件后端待建；上线后切回「订阅每周精选」）。剩余待办：X 账号 handle 确认 + 候选名单邮箱存储方案（工程侧，见 Linus）。
2. **手机端底部常驻导航栏**是否上线（§4.3）——影响首屏可用高度。
3. **深色模式**是否纳入 v1（当前列为非目标）。
4. **"为什么值得看"内容生产**：编辑人力/流程如何保障（数据依赖，非纯设计问题，涉及运营）。
5. **多语言**：是否需要英文版界面（当前中文优先，专有名词保留英文）。

---

## 10. 交付物与参考

- 本文档（design-spec）为规范唯一事实来源。
- **Stitch 视觉参考稿**（已生成，见本 issue 的 work products / `design/mockups/`）：
  - 桌面端首页 `design/mockups/home-desktop.png`（+ `.html`）
  - 手机端首页 `design/mockups/home-mobile.png`（+ `.html`）
  - 视频目录页 `design/mockups/catalog-desktop.png`（+ `.html`）
  - Stitch 项目：`projects/14067259509714201096`，设计系统 `assets/5093209118306331965`（Compass Editorial）。
- 参考稿仅作**布局与 token 参考，非生产代码**。注意：
  - **内容真实性（已清理 2026-07-14）**：mockup 内所有虚构/越界内容已删除——移除 OpenAI DevDay 及 Sam Altman / Andrej Karpathy / Andrew Ng 等真实人物示例与其肖像图。占位内容统一以「示例讲者」「示例内容 / Sample content · 非真实数据」明确标注。实际数据来源仅限 **AI Engineer / Cursor Compile / Figma Config** 三场大会；生产实现不得引入未经核实的讲者、标题或其他大会名称（**需真实数据验证**）。
  - 目录卡的讲者大图为参考稿自行添加；本规范保持**文本/阅读优先**，缩略图为可选项、不得喧宾夺主（见 §5.2/§3.1）。
  - 首页时间预算区在参考稿用深色反底块做区隔，属可接受的区块对比；正式实现避免大面积重色与强投影（见 §3.5）。
- 工程可行性问题（如混排空格实现、无限滚动性能、URL 筛选状态同步）请由 Linus 在 issue 评论中提出。

*— Jobs，产品设计*
